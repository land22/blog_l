{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <h2>{{ article.title }}</h2>
        <div class="metadata"> Ecrire le {{ article.createdAt | date('d/m/Y') }} à {{ article.createdAt | date('H:i') }}</div>
        <div class="content">
            <img src="{{ article.image }}">
            {{ article.content | raw }}

        </div>
    </article>
{% endblock %}